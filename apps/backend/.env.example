# URL de conexão do Prisma ao rodar o backend LOCALMENTE (fora do Docker).
# Host = localhost (no Docker, o compose injeta a URL com host "db").
DATABASE_URL="postgresql://user:password@localhost:5432/database?schema=public"
